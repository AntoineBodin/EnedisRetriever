services:
  myownagent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: myownagent
    ports:
      - "127.0.0.1:8080:8080"
    env_file:
      - .env
    restart: unless-stopped